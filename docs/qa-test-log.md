# QA 테스트 로그

| 테스트명 | 심각도 | 목적 | 재현 방법 | 기대 결과 | 실제 결과 | 수정 결과 | 날짜 |
|---|---|---|---|---|---|---|---|
| feature/4-일정등록 폼 검증 | - | 일정 등록 폼의 필수 필드 검증 및 localStorage 저장 기능 확인 | 제목/날짜 빈 값 제출 시도, time/memo 빈 값 상태에서 정상 제출 시도, localStorage 저장 확인 | 필수 필드 빈 값 시 에러 표시 및 제출 차단, time/memo는 선택 입력으로 정상 동작, 데이터가 localStorage에 올바른 형식으로 저장됨, aria-invalid 속성 동적 변경 | 모든 검증 통과. 경미 이슈 1건 발견: date 필드 형식 검증 없음(HTML type='date' 피커로 보장되므로 실제 영향 무시할 수 있는 수준) | 통과 | 2026-08-25 |
| feature/5-캘린더 월간 뷰 | - | 월간 달력 렌더링, 날짜 비교 정확성, 마커 표시, 날짜 필터링, 링크 생성 검증 | npm run lint/build/dev 성공 확인, date-fns 그리드 계산 (다양한 달/윤년/월 경계) 검증, "YYYY-MM-DD" 문자열 비교 추적, 초기 선택 날짜 설정, 마커 Set 생성 검증, getSchedulesByDate 필터링 로직, href 경로 생성 | (1) 요일 라벨 7개 렌더링, (2) 각 달의 그리드가 정확히 7의 배수일 (완전한 주 단위), (3) 월 경계에서 연도 변경 정확 (1월 -1달=전년 12월, 12월 +1달=차년 1월), (4) 모든 날짜 비교가 "yyyy-MM-dd" 포맷으로 타임존 버그 없음, (5) 마커가 scheduledDateSet 기준 정확한 날에만 표시, (6) 클릭한 날짜의 일정만 필터링됨, (7) Link href가 /schedule/{id} 형식 | 모든 검증 통과. lint/tsc/build 모두 성공. /calendar 200 응답, HTML 렌더링 정상. date-fns 그리드: 2026년 1월(35일), 2월(28일), 윤년 2월(35일), 8월(42일), 12월(35일) 모두 7의 배수 확인. 월 경계 엣지 케이스: 연도 변경 정확. 문자열 비교 추적: selectedDateKey, dateKey, scheduledDateSet 모두 "yyyy-MM-dd" 사용으로 타임존 이슈 없음. useSchedules 훅: isLoaded 상태 정상 작동. 성능 최적화 개선 가능 (days 배열 재생성, format 루프) 하지만 기능상 문제 없음. | 통과 | 2026-08-26 |
| feature/6-일정상세 조회/수정/삭제 | - | 선택한 일정 상세 조회, ScheduleForm 재사용으로 수정 기능, AlertDialog 기반 삭제 확인 팝업, 삭제 후 리다이렉트 검증 | npm run lint/tsc/build 성공, 조회: useParams<{ id }>로 라우팅 파라미터 획득, schedules.find(s => s.id === id) id 일치 검사 정상 여부, 수정: ScheduleForm defaultValues에 schedule.time ?? "" 로 null→"" 변환, submit 시 editSchedule(id, input) 호출, isEditing 상태 toggle, 삭제: AlertDialogTrigger + AlertDialogAction 구조, handleDelete 호출 시 removeSchedule(id) + router.push("/calendar"), Node 시뮬레이션: localStorage 일정 생성→수정→삭제 전체 흐름, time이 null일 때 "설정 안 함" 표시, memo가 ""일 때 "메모 없음" 표시, memo에 특수문자(<script>, 따옴표 등) 입력 시 XSS 방지(React 자동 이스케이프), 존재하지 않는 id 조회 시 "일정을 찾을 수 없습니다" 표시 | (1) sched-detail-1: 조회 UI 정상 렌더링, 모든 필드(제목/날짜/시간/메모) 표시 | (2) sched-detail-2: 수정 폼에 기존값 정확히 전달(time null→"" 변환 확인), 제출 후 isEditing 복귀 | (3) sched-detail-3/4: AlertDialog 팝업 구조 정상, "삭제" 클릭 시 handleDelete 실행 | (4) 삭제 완료 후 /calendar로 리다이렉트 | (5) id 불일치 시 "일정을 찾을 수 없습니다" 안내 표시 | 모든 검증 통과. lint/tsc/build 성공. useParams/useSchedules/ScheduleForm/storage.ts 타입 안전 확인. Node 시뮬레이션: 일정 생성→조회→수정→재조회→삭제→조회 전체 흐름에서 localStorage 상태 정확히 반영. time null 유지, memo 빈 문자열 유지, 특수문자 JSON 직렬화/역직렬화 정상. 기대 동작 모두 구현됨. | 통과 (경미 이슈 1건: editSchedule 호출 반환값 미검사로 실패 시 토스트만 표시되고 실제 수정 실패를 감지하지 못함, 다만 MVP 범위의 로컬 저장소만 사용하고 동시성 문제는 드물어 현 수준에서 허용 가능) | 2026-08-26 |
| feature/7-용돈목록 | - | 용돈 목록 조회(money-list-1), 수입/지출 등록 모달(money-list-2), 수입/지출 구분 표시(money-list-3), 상세 화면 링크(money-list-4) 검증 | npm run lint/tsc/build 성공, 폼 검증: 항목명/날짜/금액 각각 비어있음 테스트, 금액 0/음수/-1000 테스트, Number.isInteger 정수 검증, 특수문자/긴 텍스트/과학표기법 엣지 케이스, 수입/지출 토글 type 변경 확인, localStorage 저장 흐름(폼제출→addMoneyEntry→createMoneyEntry→localStorage), 목록 정렬(date 내림차순, 같은 날은 createdAt 역순), 배지 색상 클래스 분기(bg-income-green/10 vs bg-expense-red/10), 링크 href 생성(/money/[id] 형식) | (1) money-list-1: 목록이 정렬된 순서로 렌더링 | (2) money-list-2: 폼 필드(type/amount/date/title/memo)가 모두 존재, 검증 실패 시 에러 메시지 표시, 검증 통과 시 localStorage 저장 및 모달 닫기 | (3) money-list-3: 배지에 "수입"/"지출" 텍스트, 색상 분기 정상 | (4) money-list-4: 각 항목 Link href가 /money/{id} 형식 | (5) 메모는 선택 입력 | (6) truncate로 긴 제목도 레이아웃 유지 | 모든 검증 통과. lint/tsc/build 성공. /money 200 응답, "용돈 기입장" 제목과 "새 항목 추가" 버튼 HTML 렌더링 확인. 폼 검증 로직(Node 시뮬레이션): 13/13 테스트 통과 (항목명/날짜/금액 필수, 금액 0 이하/정수 아님 거부, NaN/소수/과학표기법 등 엣지 케이스 정상 처리). localStorage 흐름(Node 시뮬레이션): 폼입력→createMoneyEntry→JSON 직렬화→저장→조회 전체 일치 확인, 다중 항목 추가 시 정렬 정상(date 내림차순). 배지 color class, 링크 href, title truncate 코드 정독 완료. 타입 안전(MoneyEntryInput, MoneyEntry) 확인. | 통과 | 2026-08-26 |
