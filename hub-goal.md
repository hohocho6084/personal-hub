# 개인 허브 (personal-hub) 목표 문서

## 1. 프로젝트 개요

**한 줄 요약**: 일정 관리와 용돈(수입/지출) 기입을 한 곳에서 처리하는 개인용 웹 대시보드.

**배경 및 동기(Motivation)**:
여러 앱과 서비스에 흩어져 있는 "오늘 뭐 하기로 했는지"와 "돈을 얼마나 썼는지"를
한 화면에서 확인하고 싶다는 개인적인 필요에서 출발한 프로젝트다. 별도의 회원가입이나
로그인 없이, 내 컴퓨터/브라우저 안에서만 동작하는 가벼운 개인용 도구를 만드는 것이
목표다. 외부 서비스(구글 캘린더 등) 연동은 나중에 필요성이 검증되면 2단계에서
추가하기로 하고, 우선은 "직접 입력하고 직접 확인하는" 최소 기능부터 만든다.

## 2. 타겟 사용자

- 사용자 본인(1인) 전용. 다른 사람과 공유하거나 여러 계정을 지원할 필요 없음.
- 별도의 페르소나 정의나 사용자 리서치는 필요하지 않음 (본인이 곧 유일한 사용자).

## 3. 1단계(MVP) 범위 — 화면 목록과 역할

아래 6개 화면으로 MVP(최소기능제품, 우선 만들어서 검증할 최소 단위의 제품)를 구성한다.

| # | 화면 | 역할 |
|---|------|------|
| 1 | 대시보드 | 앱 진입 시 보이는 메인 화면. 오늘 일정과 최근 용돈 기입 내역 요약을 한눈에 보여줌 |
| 2 | 일정 등록 | 새 일정을 입력하는 폼. 제목, 날짜, 시간, 메모 등의 항목 포함 |
| 3 | 캘린더 | 월간 뷰(한 달 달력 형태)로 등록된 일정들을 날짜별로 확인 |
| 4 | 일정 상세페이지 | 캘린더나 목록에서 특정 일정을 클릭했을 때 상세 내용을 확인·수정·삭제 |
| 5 | 용돈 기입장 (목록) | 수입/지출 내역을 리스트 형태로 확인 |
| 6 | 용돈 기입장 (상세) | 특정 수입/지출 항목을 클릭했을 때 상세 내용을 확인·수정·삭제 |
| 7 | 데이터 백업 (내보내기/가져오기) | 대시보드 등에 배치되는 기능. 전체 일정·용돈 데이터를 JSON 파일로 내보내기(다운로드)하고, 그 파일을 다시 불러와(업로드) 복원 |

### 3-1. 데이터 백업(Export/Import) 기능 상세

localStorage(브라우저 안에만 저장되는 개인용 저장 공간)만 사용할 경우 브라우저 데이터
삭제나 기기 변경 시 전체 데이터가 유실될 위험이 있다. 이에 대한 대응으로 아래 기능을
1단계 MVP 범위에 추가한다.

- **내보내기(Export)**: 버튼 클릭 시 현재 저장된 전체 일정·용돈 데이터를 하나의 JSON
  파일로 만들어 다운로드한다.
- **가져오기(Import)**: 사용자가 내보내기로 저장해둔 JSON 파일을 업로드하면, 그 안의
  일정·용돈 데이터를 불러와 복원한다.
- **수동 실행 방식**: 자동 백업이 아니며, 사용자가 필요할 때 직접 버튼을 눌러 실행하는
  방식이다.
- **서버 없음**: 이 기능은 서버나 데이터베이스 없이 브라우저 기능(파일 다운로드/업로드)만으로
  구현한다. 자동으로 매일 서버에 백업하는 것은 2단계 범위로 남겨둔다.
- **배치 위치**: 별도 화면을 새로 만들기보다, 대시보드 등 자연스러운 위치에 버튼/메뉴
  형태로 배치한다.

## 4. 명시적 제외 범위 (2단계로 미룸)

다음 항목들은 이번 MVP 범위에 포함하지 않으며, 필요성이 검증된 이후 별도 프로젝트
단계(2단계)에서 다시 논의한다.

- 구글 캘린더 연동 (외부 캘린더와의 동기화)
- 지메일(Gmail) 연동
- 로그인/회원가입, 다중 사용자 지원
- 모바일 화면 최적화(반응형 대응) — 이번 단계의 우선순위 아님
- 서버/백엔드, 데이터베이스 구축
- 알림(리마인더), 통계/리포트 등 부가 기능

## 5. 기술적 제약사항

- **로그인 없음**: 별도의 인증/회원 시스템을 두지 않는다.
- **로컬 데이터 저장(localStorage)**: 모든 일정·용돈 데이터는 브라우저의 localStorage(브라우저 안에만
  저장되는 개인용 저장 공간, 서버로 전송되지 않음)에 저장한다.
- **서버 없음**: 별도의 백엔드 서버나 API 서버를 두지 않는다.
- **프론트엔드 단독**: 순수 프론트엔드(화면을 그리고 사용자 입력을 처리하는 부분)만으로 동작하는
  구조로 만든다.
- **플랫폼**: 웹 기반, 데스크톱 화면(PC 브라우저) 우선. 모바일 반응형 대응은 이번 범위의
  우선순위가 아니다.

## 6. 성공 기준 (MVP "완료"의 정의)

아래 조건을 모두 만족하면 1단계 MVP가 "완료"된 것으로 본다.

1. 사용자가 로그인 없이 웹 브라우저에서 앱에 접속해 바로 사용할 수 있다.
2. 대시보드에서 오늘 일정과 최근 용돈 기입 요약을 한눈에 확인할 수 있다.
3. 일정을 새로 등록(제목/날짜/시간/메모)할 수 있고, 캘린더 월간 뷰에서 날짜별로 확인할 수 있다.
4. 캘린더나 목록에서 특정 일정을 클릭해 상세 내용을 확인·수정·삭제할 수 있다.
5. 수입/지출 내역을 목록으로 확인할 수 있고, 특정 항목을 클릭해 상세 내용을 확인·수정·삭제할 수 있다.
6. 브라우저를 껐다 켜도(재접속해도) localStorage에 저장된 일정·용돈 데이터가 유지된다.
7. 전체 일정·용돈 데이터를 JSON 파일로 내보내기(다운로드)할 수 있다.
8. 내보내기한 JSON 파일을 다시 불러와(업로드) 데이터를 복원할 수 있다.
9. 서버나 외부 서비스(구글 캘린더 등) 없이도 위 1~8번 기능이 모두 정상 동작한다.
