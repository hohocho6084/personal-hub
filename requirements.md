# 개인 허브 (personal-hub) 요구사항정의서

> 근거 문서: `hub-goal.md`(프로젝트 목표 문서, 컨펌 완료), `feature-checklist.json`(1단계 MVP 기능 체크리스트, 8개 카테고리 38개 항목, 컨펌 완료)
> 이 프로젝트는 사용자 1인이 진행하는 개인용 프로젝트로, 시장조사(market-researcher) 단계는 생략했습니다. (`docs/market-research.md` 없음 — CEO 지시서에 따른 의도된 생략)

---

## 1. 배경 및 목적

여러 앱과 서비스에 흩어져 있는 "오늘 뭐 하기로 했는지"(일정)와 "돈을 얼마나 썼는지"(용돈 기입)를
한 화면에서 확인하고 싶다는 개인적인 필요에서 출발한 프로젝트다.

- 별도의 회원가입/로그인 없이, 사용자의 브라우저 안에서만 동작하는 가벼운 개인용 도구를 만드는 것이 목표다.
- 외부 서비스(구글 캘린더, 지메일 등) 연동은 나중에 필요성이 검증되면 2단계에서 추가하기로 하고,
  1단계(MVP, 최소기능제품 — 우선 만들어서 검증할 최소 단위의 제품)에서는 "직접 입력하고 직접 확인하는"
  최소 기능부터 만든다.

## 2. 타겟 사용자

- 사용자 본인(1인) 전용. 다른 사람과 공유하거나 여러 계정을 지원할 필요 없음.
- 별도의 페르소나 정의나 사용자 리서치는 필요하지 않음 (본인이 곧 유일한 사용자).
- 플랫폼: 웹 기반, 데스크톱 화면(PC 브라우저) 우선. 모바일 반응형 대응은 이번 범위의 우선순위가 아님.

## 3. 핵심 기능 목록 및 우선순위 (MoSCoW)

`feature-checklist.json`의 38개 항목은 이미 1단계 MVP 범위로 컨펌된 항목이다. 여기서는 새로운 기능을
추가/삭제하지 않고, 구현 순서를 정하기 위해 성공 기준과의 연관도에 따라 아래와 같이 우선순위를 분류한다.

### Must Have (필수 — MVP 성공 기준에 직접 연결되는 핵심 기능)

| 카테고리 | 항목 (feature-checklist.json ID) |
|---|---|
| 공통/기반작업 | common-1, common-2, common-3, common-4 |
| 대시보드 | dash-1, dash-2, dash-3 |
| 일정 등록 | sched-add-1, sched-add-2, sched-add-3, sched-add-4, sched-add-6 |
| 캘린더 | cal-1, cal-2, cal-4, cal-5 |
| 일정 상세 | sched-detail-1, sched-detail-2, sched-detail-3 |
| 용돈 목록 | money-list-1, money-list-2, money-list-4 |
| 용돈 상세 | money-detail-1, money-detail-2, money-detail-3 |
| 데이터 백업 | backup-1, backup-2, backup-5 |

### Should Have (중요 — 있으면 완성도와 안정성이 크게 올라가는 기능)

| 카테고리 | 항목 (ID) | 비고 |
|---|---|---|
| 일정 등록 | sched-add-5 | 필수 항목(제목/날짜) 누락 시 안내 문구 |
| 캘린더 | cal-3 | 일정 있는 날짜에 마커/뱃지 표시 |
| 일정 상세 | sched-detail-4 | 삭제 시 확인 팝업 |
| 용돈 목록 | money-list-3 | 수입/지출 구분 표시 |
| 용돈 상세 | money-detail-4 | 삭제 시 확인 팝업 |
| 대시보드 | dash-4 | 백업 버튼/메뉴 노출 위치 |
| 데이터 백업 | backup-3, backup-4 | 덮어쓰기 안내, 오류 파일 안내 |

### Could Have / Won't Have (이번 범위 아님)

- Could Have에 해당하는 별도 항목은 없음 (38개 항목이 이미 MVP 최소 범위로 조정되어 있음).
- Won't Have(2단계로 명시적으로 미룬 범위)는 "6. 명시적 제외 범위" 참고.

## 4. 기능 요구사항 (화면 단위 상세)

### 4-1. 공통

- 로그인/회원가입 없이 바로 앱에 접속해 사용할 수 있어야 한다. (common-1)
- 일정·용돈 데이터는 브라우저 localStorage에 저장되어 재접속 시에도 유지되어야 한다. (common-2)
- 화면 간 이동(대시보드, 일정 등록, 캘린더, 일정 상세, 용돈 목록, 용돈 상세)이 메뉴/링크로 가능해야 한다. (common-3)
- 서버나 외부 서비스 연동 없이 모든 기능이 동작해야 한다. (common-4)

### 4-2. 대시보드 (앱 진입 시 메인 화면)

- 오늘 날짜의 일정을 요약해서 보여준다. (dash-1)
- 최근 용돈 기입 내역을 요약해서 보여준다. (dash-2)
- 캘린더, 용돈 기입장 등 다른 화면으로 바로 이동할 수 있다. (dash-3)
- 데이터 백업(내보내기/가져오기) 버튼 또는 메뉴가 노출된다. (dash-4)

### 4-3. 일정 등록

- 제목, 날짜, 시간, 메모(설명)를 입력할 수 있다. (sched-add-1~4)
- 필수 항목(제목/날짜) 누락 시 저장되지 않고 안내 문구가 표시된다. (sched-add-5)
- 저장하면 localStorage에 반영되고 캘린더에서 확인 가능하다. (sched-add-6)

### 4-4. 캘린더 (월간 뷰)

- 월간(한 달) 달력 형태로 화면을 볼 수 있다. (cal-1)
- 이전 달/다음 달로 이동할 수 있다. (cal-2)
- 일정이 있는 날짜에 표시(마커/뱃지 등)가 보인다. (cal-3)
- 특정 날짜를 클릭하면 해당 날짜의 일정 목록을 확인할 수 있다. (cal-4)
- 일정을 클릭하면 일정 상세 화면으로 이동한다. (cal-5)

### 4-5. 일정 상세

- 선택한 일정의 제목/날짜/시간/메모를 확인할 수 있다. (sched-detail-1)
- 일정 내용을 수정할 수 있다. (sched-detail-2)
- 일정을 삭제할 수 있으며, 삭제 시 확인 팝업이 표시된다. (sched-detail-3, sched-detail-4)

### 4-6. 용돈 기입장 (목록)

- 수입/지출 내역을 리스트 형태로 확인할 수 있다. (money-list-1)
- 새 수입/지출 항목을 추가(금액/날짜/항목명/메모 등)할 수 있다. (money-list-2)
- 목록에서 수입/지출 여부를 구분해서 볼 수 있다. (money-list-3)
- 특정 항목을 클릭하면 용돈 상세 화면으로 이동한다. (money-list-4)

### 4-7. 용돈 기입장 (상세)

- 선택한 항목의 상세 내용(금액/날짜/항목명/메모)을 확인할 수 있다. (money-detail-1)
- 항목 내용을 수정할 수 있다. (money-detail-2)
- 항목을 삭제할 수 있으며, 삭제 시 확인 팝업이 표시된다. (money-detail-3, money-detail-4)

### 4-8. 데이터 백업 (내보내기/가져오기)

localStorage만 사용할 경우 브라우저 데이터 삭제나 기기 변경 시 전체 데이터가 유실될 위험이 있어
1단계 MVP 범위에 포함된 기능이다. 별도 화면을 새로 만들지 않고, 대시보드 등 자연스러운 위치에
버튼/메뉴 형태로 배치한다. (dash-4)

- **내보내기(Export)**: 버튼 클릭 시 현재 저장된 전체 일정·용돈 데이터를 하나의 JSON 파일로 만들어
  다운로드한다. (backup-1)
- **가져오기(Import)**: 내보내기로 저장해둔 JSON 파일을 업로드하면 일정·용돈 데이터를 불러와
  복원한다. (backup-2)
- 가져오기 시 기존 데이터를 덮어쓰는 것에 대한 안내/확인 문구가 표시된다. (backup-3)
- 형식이 올바르지 않은 파일을 업로드하면 오류 안내가 표시된다. (backup-4)
- 자동 백업이 아니며 서버/데이터베이스 없이 브라우저 기능(파일 다운로드/업로드)만으로, 사용자가
  필요할 때 직접 버튼을 눌러 실행하는 방식이다. (backup-5)

## 5. 비기능 요구사항

| 구분 | 내용 |
|---|---|
| 인증/보안 | 별도의 인증/회원 시스템 없음. 데이터는 사용자 브라우저 밖으로 전송되지 않음 |
| 데이터 저장 | 모든 일정·용돈 데이터는 브라우저 localStorage에 저장 (서버로 전송되지 않음) |
| 아키텍처 | 서버/백엔드/데이터베이스 없이 순수 프론트엔드(화면을 그리고 사용자 입력을 처리하는 부분)만으로 동작 |
| 플랫폼 | 웹 기반, 데스크톱(PC 브라우저) 우선. 모바일 반응형은 이번 범위 우선순위 아님 |
| 데이터 지속성 | 브라우저를 껐다 켜도(재접속해도) localStorage에 저장된 데이터가 유지되어야 함 |
| 데이터 이식성 | JSON 파일 내보내기/가져오기를 통해 브라우저 변경·데이터 유실 상황에 수동으로 대응 가능해야 함 |
| 가용성 | 서버나 외부 서비스 없이도 모든 핵심 기능이 정상 동작해야 함 (오프라인에서도 사용 가능한 구조가 바람직) |

## 6. 명시적 제외 범위 (2단계로 미룸)

다음 항목들은 이번 MVP 범위에 포함하지 않으며, 필요성이 검증된 이후 별도 프로젝트 단계(2단계)에서
다시 논의한다.

- 구글 캘린더 연동 (외부 캘린더와의 동기화)
- 지메일(Gmail) 연동
- 로그인/회원가입, 다중 사용자 지원
- 모바일 화면 최적화(반응형 대응)
- 서버/백엔드, 데이터베이스 구축
- 알림(리마인더), 통계/리포트 등 부가 기능

## 7. 제약사항 및 기술 관련 참고사항

- **로그인 없음**: 별도의 인증/회원 시스템을 두지 않는다.
- **로컬 데이터 저장(localStorage)**: 모든 일정·용돈 데이터는 브라우저의 localStorage에 저장한다.
- **서버 없음**: 별도의 백엔드 서버나 API 서버를 두지 않는다. 프론트엔드 단독으로 동작한다.
- **플랫폼**: 웹 기반, 데스크톱 화면(PC 브라우저) 우선.

### 2단계 확장 관련 참고사항 (이번 범위 아님, 기록 목적)

- 2단계(이번 MVP 범위 밖)에 구글 캘린더/지메일 연동이 예정되어 있다.
- 이 때문에 향후 서버 기능(외부 API 인증·연동 처리 등)이 필요해질 가능성을 고려해, 기술 스택 후보로
  Next.js(React 기반 풀스택 프레임워크, 서버 기능도 함께 지원)가 검토되고 있다.
- **단, 최종 기술 스택 확정은 이번 요구사항정의서 단계의 범위가 아니다.** 이 문서에서는 참고사항으로만
  기록하며, 실제 기술 스택 결정은 다음 단계인 개발팀장(dev-lead)의 기술 설계 단계에서 이루어진다.
  기획팀장은 이 문서에서 기술 스택을 임의로 확정하지 않는다.

## 8. 디자인 참고사항

- 추후 UI 구현은 shadcn(React 컴포넌트 라이브러리) 기반으로 진행할 예정이다.
- 프로젝트 루트의 `Design.md` 문서("Calendly.com 스타일 레퍼런스" — 네이비 잉크(`#0b3558`) 텍스트,
  단일 블루 강조색(`#006bff`), 8px 버튼 라운드/16px 프로덕트 카드 라운드/24px 카드 라운드 등을 정의한
  디자인 토큰 문서)를 준수해서 개발한다.
- **단, 이 디자인 작업 자체는 이번 요구사항정의서 단계의 범위가 아니다.** 실제 톤앤매너 적용은 와이어프레임
  확정 이후 별도의 디자인/구현 단계(ui-designer, 개발자)에서 이루어지며, 이 문서에서는 참고 방향만
  기록한다.

## 9. 성공 기준 (MVP "완료"의 정의)

아래 조건을 모두 만족하면 1단계 MVP가 "완료"된 것으로 본다.

1. 사용자가 로그인 없이 웹 브라우저에서 앱에 접속해 바로 사용할 수 있다.
2. 대시보드에서 오늘 일정과 최근 용돈 기입 요약을 한눈에 확인할 수 있다.
3. 일정을 새로 등록(제목/날짜/시간/메모)할 수 있고, 캘린더 월간 뷰에서 날짜별로 확인할 수 있다.
4. 캘린더나 목록에서 특정 일정을 클릭해 상세 내용을 확인·수정·삭제할 수 있다.
5. 수입/지출 내역을 목록으로 확인할 수 있고, 특정 항목을 클릭해 상세 내용을 확인·수정·삭제할 수 있다.
6. 브라우저를 껐다 켜도(재접속해도) localStorage에 저장된 일정·용돈 데이터가 유지된다.
7. 전체 일정·용돈 데이터를 JSON 파일로 내보내기(다운로드)할 수 있다.
8. 내보내기한 JSON 파일을 다시 불러와(업로드) 데이터를 복원할 수 있다.
9. 서버나 외부 서비스(구글 캘린더 등) 없이도 위 1~8번 기능이 모두 정상 동작한다.
